import episodeCode from "../utils/episodeCode";
import { IEpisode } from "../episodesInterface";

function singleEpisodeView(episode: IEpisode): JSX.Element {
    const episodeCodeName = episodeCode(episode);
    return (
        <>
            
        </>
    )
}